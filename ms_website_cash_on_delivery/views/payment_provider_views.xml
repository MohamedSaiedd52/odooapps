<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="payment_provider_form_cod" model="ir.ui.view">
        <field name="name">payment.provider.form.cod</field>
        <field name="model">payment.provider</field>
        <field name="inherit_id" ref="payment.payment_provider_form"/>
        <field name="arch" type="xml">
            <xpath expr="//page[@name='configuration']" position="inside">
                <group string="Cash on Delivery"
                       name="cod_configuration"
                       invisible="custom_mode != 'cash_on_delivery'">
                    <field name="cod_min_order_amount"/>
                    <field name="cod_fee_fixed"/>
                    <field name="cod_fee_percent"/>
                    <field name="cod_allowed_zip_codes"
                           placeholder="e.g. 11511, 21532 — empty allows all"/>
                </group>
            </xpath>
        </field>
    </record>

</odoo>
